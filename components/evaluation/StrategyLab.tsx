"use client"
import {useState} from "react"
import type {EvaluationStrategy,StrategyBenchmark,StrategyType,StrategyLatencyType} from "@/types/evaluation-strategy"
import type {EvaluationQuery} from "@/types/evaluation"
import {Card,CardContent,CardHeader,CardTitle} from "@/components/ui/card"
import {Button} from "@/components/ui/button"
import {Checkbox} from "@/components/ui/checkbox"
import {Input} from "@/components/ui/input"
import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from "@/components/ui/select"
const metric=(value:number|null|undefined)=>value===null||value===undefined?"Unavailable":value.toFixed(3),ms=(value:number|null)=>value===null?"Unavailable":`${value.toFixed(1)} ms`
export function StrategyLab({strategies,queries,benchmark,onCreate,onBenchmark,loading}:{strategies:EvaluationStrategy[];queries:EvaluationQuery[];benchmark?:StrategyBenchmark;onCreate:(input:unknown)=>Promise<void>;onBenchmark:(input:unknown)=>Promise<void>;loading?:boolean}){const[name,setName]=useState(""),[type,setType]=useState<StrategyType>("external"),[latencyType,setLatencyType]=useState<StrategyLatencyType>("end_to_end"),[strategyIds,setStrategyIds]=useState<string[]>([]),[queryIds,setQueryIds]=useState<string[]>([]);return <div className="space-y-6"><header><h1 className="text-3xl font-bold">Strategy Lab</h1><p className="text-muted-foreground">Compare quality, latency, hard-negative behavior, and available stage evidence on one frozen benchmark. No combined score or universal “best” claim is produced.</p></header><Card><CardHeader><CardTitle>Register Strategy</CardTitle></CardHeader><CardContent className="grid gap-3 md:grid-cols-4"><Input aria-label="Strategy name" value={name} onChange={event=>setName(event.target.value)} placeholder="Strategy name"/><Select value={type} onValueChange={value=>setType(value as StrategyType)}><SelectTrigger aria-label="Strategy type"><SelectValue/></SelectTrigger><SelectContent>{["keyword","dense","hybrid","reranked","external","custom"].map(value=><SelectItem value={value} key={value}>{value}</SelectItem>)}</SelectContent></Select><Select value={latencyType} onValueChange={value=>setLatencyType(value as StrategyLatencyType)}><SelectTrigger aria-label="Latency type"><SelectValue/></SelectTrigger><SelectContent>{["end_to_end","retrieval_only","rerank_only","custom"].map(value=><SelectItem value={value} key={value}>{value}</SelectItem>)}</SelectContent></Select><Button disabled={!name||loading} onClick={async()=>{await onCreate({name,type,latencyType,configuration:{}});setName("")}}>Create Strategy</Button><p className="md:col-span-4 text-sm text-muted-foreground">Imported execution results are API-first in Phase 12. The server derives ranks and canonical identities; see the documented strategy-execution format.</p></CardContent></Card><Card><CardHeader><CardTitle>Benchmark Selector</CardTitle></CardHeader><CardContent className="space-y-4"><div><strong>Strategies (2–10)</strong>{strategies.map(strategy=><label className="flex gap-2" key={strategy.id}><Checkbox checked={strategyIds.includes(strategy.id)} onCheckedChange={checked=>setStrategyIds(ids=>checked?[...ids,strategy.id]:ids.filter(id=>id!==strategy.id))}/>{strategy.name} · {strategy.type} · config {strategy.configHash.slice(0,8)}</label>)}</div><div><strong>Frozen benchmark queries</strong>{queries.map(query=><label className="flex gap-2" key={query.id}><Checkbox checked={queryIds.includes(query.id)} onCheckedChange={checked=>setQueryIds(ids=>checked?[...ids,query.id]:ids.filter(id=>id!==query.id))}/>{query.name}</label>)}</div><Button disabled={loading||strategyIds.length<2||!queryIds.length} onClick={()=>onBenchmark({strategyIds,evaluationQueryIds:queryIds,cutoffs:[5,10,50]})}>Run Strategy Benchmark</Button></CardContent></Card>{benchmark&&<><Card><CardHeader><CardTitle>{benchmark.leaderboardLabel}</CardTitle></CardHeader><CardContent className="overflow-x-auto"><table className="w-full text-sm"><thead><tr><th>Strategy</th><th>nDCG@10</th><th>Recall@10</th><th>MRR</th><th>Hit@10</th><th>Precision@10</th><th>Coverage@10</th><th>Mean latency</th><th>p95 latency</th><th>Hard negatives</th><th>Stage evidence</th></tr></thead><tbody>{benchmark.leaderboardStrategyIds.map(id=>{const result=benchmark.results.find(item=>item.strategy.id===id)!,at10=result.aggregate.byCutoff.find(item=>item.cutoff===10);return <tr key={id}><td>{result.strategy.name}</td><td>{metric(at10?.meanNdcg.value)}</td><td>{metric(at10?.meanBenchmarkRecall.value)}</td><td>{metric(result.aggregate.mrr.value)}</td><td>{metric(at10?.meanHit.value)}</td><td>{metric(at10?.meanJudgedPrecision.value)}</td><td>{metric(at10?.meanJudgmentCoverage.value)}</td><td>{ms(result.latency.mean)}</td><td>{ms(result.latency.p95)}</td><td>{result.errors.hardNegativeCandidateCount}</td><td>{result.stage.available?"Available":"Unavailable"}</td></tr>})}</tbody></table></CardContent></Card>{benchmark.comparisons.map(comparison=><Card key={`${comparison.strategyAId}-${comparison.strategyBId}`}><CardHeader><CardTitle>Pairwise: {strategies.find(s=>s.id===comparison.strategyAId)?.name} vs {strategies.find(s=>s.id===comparison.strategyBId)?.name}</CardTitle></CardHeader><CardContent><p>Wins {comparison.wins} · Losses {comparison.losses} · Ties {comparison.ties} · Unavailable {comparison.unavailable}</p><p>Mean quality delta: {metric(comparison.metricDelta)} · Latency delta: {ms(comparison.latencyDeltaMs)} · Hard-negative difference: {comparison.hardNegativeDelta}</p>{comparison.warnings.map(warning=><p key={warning}>{warning}</p>)}<div className="grid gap-3 md:grid-cols-2"><div><strong>Biggest wins</strong>{comparison.largestWins.map(item=><p key={item.evaluationQueryId}>{item.queryText}: {metric(item.before)} → {metric(item.after)} ({item.delta!>=0?"+":""}{metric(item.delta)})</p>)}</div><div><strong>Biggest regressions</strong>{comparison.largestLosses.map(item=><p key={item.evaluationQueryId}>{item.queryText}: {metric(item.before)} → {metric(item.after)} ({metric(item.delta)})</p>)}</div></div></CardContent></Card>)}</>}</div>}
