import React from "react"
import {renderToStaticMarkup} from "react-dom/server"
import {StrategyLab} from "../StrategyLab"

const strategy=(id:string,name:string)=>({id,name,type:"external",description:"",provider:null,model:null,configuration:{},configHash:id.repeat(64).slice(0,64),latencyType:"end_to_end",status:"active",executionCount:1,createdAt:new Date(),createdByUserId:"owner",archivedAt:null})
const metric=(value:number)=>({value,available:true,numerator:1,denominator:1})
const aggregate={queryCount:1,mrr:metric(.8),byCutoff:[{cutoff:10,meanNdcg:metric(.7),meanBenchmarkRecall:metric(.8),meanHit:metric(1),meanJudgedPrecision:metric(.6),meanJudgmentCoverage:metric(.5)}]}
describe("StrategyLab",()=>{test("renders accessible benchmark tradeoffs and regressions without a universal best claim",()=>{const strategies=[strategy("a","Dense"),strategy("b","Hybrid")],benchmark={benchmarkPolicyVersion:"1",metricVersion:"1",datasetVersionId:"d",strategyIds:["a","b"],evaluationQueryIds:["q"],cutoffs:[10],results:strategies.map((item,index)=>({strategy:item,executionIds:[`e${index}`],aggregate,queries:[],latency:{available:true,latencyType:"end_to_end",count:1,mean:20+index,median:20+index,p95:30+index,min:20+index,max:30+index},errors:{hardNegativeCandidateCount:index,highCriticalCount:0,top5Grade0Count:0,outranksGrade2Count:0,queriesWithHighCritical:0,candidateRatePerQuery:index,highCriticalQueryRate:0},stage:{available:index===0,candidateBenchmarkRecall:null,finalBenchmarkRecall:null,candidateToFinalRetention:null,grade2Survival:null,downstreamRelevantLoss:null,irrelevantDownstreamPromotions:0,warning:index?"Stage comparison unavailable":null},warnings:[]})),comparisons:[{strategyAId:"a",strategyBId:"b",primaryMetric:"nDCG@10",wins:1,losses:1,ties:0,unavailable:0,comparableQueries:2,winRate:.5,lossRate:.5,metricDelta:.1,latencyDeltaMs:1,hardNegativeDelta:1,queryOutcomes:[],largestWins:[{evaluationQueryId:"q",queryText:"query win",metric:"nDCG@10",before:.4,after:.7,delta:.3,outcome:"win"}],largestLosses:[{evaluationQueryId:"r",queryText:"query regression",metric:"nDCG@10",before:.8,after:.5,delta:-.3,outcome:"loss"}],warnings:[]}],leaderboardStrategyIds:["a","b"],leaderboardLabel:"Highest nDCG@10",warnings:[],persisted:false,createdAt:new Date(),createdByUserId:"owner"};const html=renderToStaticMarkup(<StrategyLab strategies={strategies as never} queries={[{id:"q",name:"Query"}] as never} benchmark={benchmark as never} onCreate={async()=>{}} onBenchmark={async()=>{}}/>);expect(html).toContain("Strategy Lab");expect(html).toContain("Highest nDCG@10");expect(html).toContain("p95 latency");expect(html).toContain("Hard negatives");expect(html).toContain("Unavailable");expect(html).toContain("Biggest regressions");expect(html).toContain("query regression");expect(html).not.toContain(">Best strategy<")})})
