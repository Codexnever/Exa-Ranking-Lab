import type {MetricDelta,QualityAwareInterpretation,QualityChangeDirection,QualityChangeMagnitude} from "@/types/evaluation-comparison"
export const QUALITY_COMPARISON_POLICY_VERSION="1" as const
export const QUALITY_COMPARISON_POLICY=Object.freeze({version:QUALITY_COMPARISON_POLICY_VERSION,stableEpsilon:.01,smallUpperBound:.05,moderateUpperBound:.15,coverageWarningThreshold:.5,coverageMaterialChange:.2,primaryMetricPriority:["ndcg@10","ndcg@highest-common-cutoff","mrr","benchmarkRecall@highest-common-cutoff"],cohort:"common-query-and-metric-availability-intersection",delta:"after-minus-before",sorting:"primary-metric-delta"} as const)
const rounded=(value:number)=>Math.round(value*1e12)/1e12
export function direction(delta:number|null):QualityChangeDirection{if(delta===null)return"unavailable";if(Math.abs(delta)<QUALITY_COMPARISON_POLICY.stableEpsilon)return"stable";return delta>0?"improved":"degraded"}
export function magnitude(delta:number|null):QualityChangeMagnitude{if(delta===null)return"unavailable";const value=Math.abs(delta);if(value<QUALITY_COMPARISON_POLICY.stableEpsilon)return"stable";if(value<QUALITY_COMPARISON_POLICY.smallUpperBound)return"small";if(value<QUALITY_COMPARISON_POLICY.moderateUpperBound)return"moderate";return"large"}
export function metricDelta(before:number[],after:number[]):MetricDelta{if(before.length!==after.length)throw new TypeError("Metric cohorts must be aligned");if(!before.length)return{before:null,after:null,delta:null,contributingQueryCount:0,direction:"unavailable",magnitude:"unavailable"};const mean=(values:number[])=>values.reduce((sum,value)=>sum+value,0)/values.length,b=mean(before),a=mean(after),delta=rounded(a-b);return{before:b,after:a,delta,contributingQueryCount:before.length,direction:direction(delta),magnitude:magnitude(delta)}}
export function interpretation(primary:MetricDelta,primaryMetric:string|null,recall:MetricDelta|undefined,drift?:{mean:number;substantial:boolean}):QualityAwareInterpretation{if(primary.delta===null)return{direction:"unavailable",magnitude:"unavailable",primaryMetric:null,reasonCode:"QUALITY_UNAVAILABLE",explanation:drift?"Ranking change can be measured, but relevance comparison is unavailable for the common judged cohort.":"Relevance comparison is unavailable for the common judged cohort."};const d=primary.direction,recallStable=recall?.direction==="stable"||recall?.magnitude==="small";let reasonCode=d==="degraded"?(recallStable?"ORDERING_DEGRADED_RECALL_STABLE":"QUALITY_AND_RECALL_DEGRADED"):d==="improved"?(recallStable?"ORDERING_IMPROVED_RECALL_STABLE":"QUALITY_IMPROVED"):"QUALITY_STABLE";let text=d==="degraded"?(recallStable?"Ranking quality degraded while known relevant-document retrieval remained broadly stable; this pattern suggests ordering worsened.":"Ranking quality degraded and fewer known relevant benchmark documents appeared in the evaluated top results."):d==="improved"?(recallStable?"Ranking quality improved while known relevant-document retrieval remained broadly stable; this pattern suggests ordering improved.":"Measured ranking quality improved for the common judged cohort."):"Measured benchmark relevance remained stable.";if(drift)text+=drift.substantial?(d==="stable"?" Ranking changed substantially, but measured benchmark relevance remained stable.":` Substantial ranking change coincided with ${d==="improved"?"improved":"lower"} measured relevance.`):(d==="degraded"?" Measured relevance declined despite limited overall ranking movement.":d==="improved"?" Measured relevance improved with limited overall ranking movement.":"");return{direction:d,magnitude:primary.magnitude,primaryMetric,reasonCode,explanation:text}}

