import {databases,DATABASE_ID,COLLECTIONS,ID,Query} from "@/app/server/appwrite/appwrite-server"
import type {EvaluationStrategy,StrategyExecution,StrategyExecutionDocument,StrategyExecutionSource,StrategyLatencyType,StrategyStatus,StrategyType} from "@/types/evaluation-strategy"
import {canonicalizeStageDocuments} from "./evaluation-stage-trace-calculations"
import {strategyConfigHash} from "./evaluation-strategy-calculations"
import {evaluationDatasetService,type EvaluationDatasetService} from "./evaluation-dataset-service"
import {evaluationStageTraceService,type EvaluationStageTraceService} from "./evaluation-stage-trace-service"
import {EvaluationError,invalid} from "./evaluation-errors"
import {STRATEGY_BENCHMARK_POLICY as POLICY} from "./strategy-benchmark-policy"
type Doc=Record<string,unknown>
export interface StrategyRepository{createStrategy(id:string,data:Doc):Promise<Doc>;updateStrategy(id:string,data:Doc):Promise<Doc>;getStrategy(id:string):Promise<Doc|null>;listStrategies(userId:string,includeArchived:boolean):Promise<Doc[]>;createExecution(id:string,data:Doc):Promise<Doc>;createExecutionDocument(id:string,data:Doc):Promise<Doc>;deleteExecution(id:string):Promise<void>;deleteExecutionDocuments(id:string):Promise<void>;getExecution(id:string):Promise<Doc|null>;listExecutions(datasetId:string,strategyId?:string,evaluationQueryId?:string):Promise<Doc[]>;listExecutionDocuments(executionId:string):Promise<Doc[]>}
function storage(error:unknown,action:string):never{if(error instanceof EvaluationError)throw error;if((error as{code?:number})?.code===404)throw new EvaluationError("NOT_FOUND",`${action} not found`,404);throw new EvaluationError("STORAGE_ERROR",`Failed to ${action}`,500)}
export class AppwriteStrategyRepository implements StrategyRepository{
 async createStrategy(id:string,data:Doc){try{return await databases.createDocument(DATABASE_ID,COLLECTIONS.EVALUATION_STRATEGIES,id,data) as unknown as Doc}catch(e){storage(e,"create strategy")}}
 async updateStrategy(id:string,data:Doc){try{return await databases.updateDocument(DATABASE_ID,COLLECTIONS.EVALUATION_STRATEGIES,id,data) as unknown as Doc}catch(e){storage(e,"update strategy status")}}
 async getStrategy(id:string){try{return await databases.getDocument(DATABASE_ID,COLLECTIONS.EVALUATION_STRATEGIES,id) as unknown as Doc}catch(e){if((e as{code?:number})?.code===404)return null;storage(e,"read strategy")}}
 async listStrategies(userId:string,includeArchived:boolean){try{const result=await databases.listDocuments(DATABASE_ID,COLLECTIONS.EVALUATION_STRATEGIES,[Query.equal("createdByUserId",userId),...(includeArchived?[]:[Query.equal("status","active")]),Query.orderDesc("createdAt"),Query.limit(100)]);return result.documents as unknown as Doc[]}catch(e){storage(e,"list strategies")}}
 async createExecution(id:string,data:Doc){try{return await databases.createDocument(DATABASE_ID,COLLECTIONS.EVALUATION_STRATEGY_EXECUTIONS,id,data) as unknown as Doc}catch(e){storage(e,"create strategy execution")}}
 async createExecutionDocument(id:string,data:Doc){try{return await databases.createDocument(DATABASE_ID,COLLECTIONS.EVALUATION_STRATEGY_EXECUTION_DOCUMENTS,id,data) as unknown as Doc}catch(e){storage(e,"create strategy execution document")}}
 async deleteExecution(id:string){try{await databases.deleteDocument(DATABASE_ID,COLLECTIONS.EVALUATION_STRATEGY_EXECUTIONS,id)}catch(e){if((e as{code?:number})?.code!==404)storage(e,"delete incomplete execution")}}
 async deleteExecutionDocuments(id:string){for(const doc of await this.listExecutionDocuments(id))await databases.deleteDocument(DATABASE_ID,COLLECTIONS.EVALUATION_STRATEGY_EXECUTION_DOCUMENTS,String(doc.$id))}
 async getExecution(id:string){try{return await databases.getDocument(DATABASE_ID,COLLECTIONS.EVALUATION_STRATEGY_EXECUTIONS,id) as unknown as Doc}catch(e){if((e as{code?:number})?.code===404)return null;storage(e,"read strategy execution")}}
 async listExecutions(datasetId:string,strategyId?:string,evaluationQueryId?:string){try{const result=await databases.listDocuments(DATABASE_ID,COLLECTIONS.EVALUATION_STRATEGY_EXECUTIONS,[Query.equal("datasetVersionId",datasetId),...(strategyId?[Query.equal("strategyId",strategyId)]:[]),...(evaluationQueryId?[Query.equal("evaluationQueryId",evaluationQueryId)]:[]),Query.orderDesc("createdAt"),Query.limit(500)]);return result.documents as unknown as Doc[]}catch(e){storage(e,"list strategy executions")}}
 async listExecutionDocuments(id:string){try{const result=await databases.listDocuments(DATABASE_ID,COLLECTIONS.EVALUATION_STRATEGY_EXECUTION_DOCUMENTS,[Query.equal("executionId",id),Query.orderAsc("rank"),Query.limit(500)]);return result.documents as unknown as Doc[]}catch(e){storage(e,"read strategy execution documents")}}
}
const required=(doc:Doc,key:string)=>{const value=doc[key];if(typeof value!=="string"||!value.trim())throw new TypeError(`${key} is required`);return value},nullable=(value:unknown)=>value===null||value===undefined?null:required({value},"value"),date=(value:unknown)=>{const result=new Date(String(value));if(Number.isNaN(result.getTime()))throw new TypeError("date is malformed");return result},integer=(value:unknown,name:string,min=0)=>{if(!Number.isInteger(value)||Number(value)<min)throw new TypeError(`${name} is malformed`);return Number(value)},json=(value:unknown,name:string)=>{if(typeof value!=="string")throw new TypeError(`${name} is malformed`);try{const parsed=JSON.parse(value);if(!parsed||typeof parsed!=="object"||Array.isArray(parsed))throw new Error();return parsed as Record<string,unknown>}catch{throw new TypeError(`${name} is malformed`)}}
export function transformStrategy(doc:Doc):EvaluationStrategy{const type=required(doc,"type") as StrategyType,status=required(doc,"status") as StrategyStatus,latencyType=required(doc,"latencyType") as StrategyLatencyType;if(!["keyword","dense","hybrid","reranked","external","custom"].includes(type)||!["active","archived"].includes(status)||!["end_to_end","retrieval_only","rerank_only","custom"].includes(latencyType))throw new TypeError("strategy enum is malformed");const configuration=json(doc.configurationJson,"configuration");const result={id:required(doc,"$id"),name:required(doc,"name"),type,description:String(doc.description??""),provider:nullable(doc.provider),model:nullable(doc.model),configuration,configHash:required(doc,"configHash"),latencyType,status,executionCount:integer(doc.executionCount,"executionCount"),createdAt:date(doc.createdAt),createdByUserId:required(doc,"createdByUserId"),archivedAt:doc.archivedAt?date(doc.archivedAt):null};if(strategyConfigHash(result)!==result.configHash)throw new TypeError("strategy configuration hash is inconsistent");return result}
export function transformExecution(header:Doc,docs:Doc[]):StrategyExecution{const resultCount=integer(header.resultCount,"resultCount"),documents:StrategyExecutionDocument[]=docs.map(doc=>({documentKey:required(doc,"documentKey"),canonicalUrl:required(doc,"canonicalUrl"),rawUrl:required(doc,"rawUrl"),rank:integer(doc.rank,"rank",1),score:doc.score===null||doc.score===undefined?null:Number(doc.score),scoreType:nullable(doc.scoreType),title:nullable(doc.title),domain:required(doc,"domain")}));if(documents.length!==resultCount||new Set(documents.map(d=>d.documentKey)).size!==documents.length||documents.some((d,i)=>i>0&&d.rank<=documents[i-1].rank||d.score!==null&&!Number.isFinite(d.score)))throw new TypeError("strategy execution documents are malformed");return{id:required(header,"$id"),strategyId:required(header,"strategyId"),datasetVersionId:required(header,"datasetVersionId"),evaluationQueryId:required(header,"evaluationQueryId"),sourceQueryId:required(header,"sourceQueryId"),queryText:required(header,"queryText"),source:required(header,"source") as StrategyExecutionSource,configHash:required(header,"configHash"),requestedResultCount:header.requestedResultCount===null?null:integer(header.requestedResultCount,"requestedResultCount",1),resultCount,latencyMs:header.latencyMs===null?null:Number(header.latencyMs),latencyType:required(header,"latencyType") as StrategyLatencyType,stageTraceId:nullable(header.stageTraceId),providerMetadata:json(header.providerMetadataJson,"providerMetadata"),duplicateCanonicalResultsIgnored:integer(header.duplicateCanonicalResultsIgnored,"duplicates"),documents,createdAt:date(header.createdAt),createdByUserId:required(header,"createdByUserId")}}
export class EvaluationStrategyService{
 constructor(private readonly repository:StrategyRepository=new AppwriteStrategyRepository(),private readonly datasets:EvaluationDatasetService=evaluationDatasetService,private readonly traces:EvaluationStageTraceService=evaluationStageTraceService){}
 async createStrategy(userId:string,input:unknown){const raw=this.object(input),allowed=["name","type","description","provider","model","configuration","latencyType"];if(Object.keys(raw).some(key=>!allowed.includes(key)))throw invalid("Strategy input contains unsupported fields");const name=this.text(raw.name,"name",256),type=raw.type as StrategyType,latencyType=raw.latencyType as StrategyLatencyType;if(!["keyword","dense","hybrid","reranked","external","custom"].includes(type))throw invalid("Strategy type is invalid");if(!["end_to_end","retrieval_only","rerank_only","custom"].includes(latencyType))throw invalid("latencyType is invalid");const configuration=this.configuration(raw.configuration),provider=this.optional(raw.provider),model=this.optional(raw.model),data={name,type,description:this.optional(raw.description)??"",provider,model,configurationJson:JSON.stringify(configuration),configHash:strategyConfigHash({type,provider,model,latencyType,configuration}),latencyType,status:"active",executionCount:0,createdAt:new Date().toISOString(),createdByUserId:userId,archivedAt:null};return transformStrategy(await this.repository.createStrategy(ID.unique(),data))}
 async listStrategies(userId:string,includeArchived=false){return Promise.all((await this.repository.listStrategies(userId,includeArchived)).map(async doc=>{const strategy=transformStrategy(doc);if(strategy.createdByUserId!==userId)throw new EvaluationError("INVALID_STATE","Strategy listing contains foreign records",409);return strategy}))}
 async getStrategy(userId:string,id:string){if(!id?.trim())throw invalid("Strategy ID is required");const doc=await this.repository.getStrategy(id);if(!doc)throw new EvaluationError("NOT_FOUND","Strategy not found",404);const strategy=transformStrategy(doc);if(strategy.createdByUserId!==userId)throw new EvaluationError("UNAUTHORIZED","Strategy access denied",403);return strategy}
 async archiveStrategy(userId:string,id:string){const strategy=await this.getStrategy(userId,id);if(strategy.status==="archived")return strategy;return transformStrategy(await this.repository.updateStrategy(id,{status:"archived",archivedAt:new Date().toISOString()}))}
 async createExecution(userId:string,datasetId:string,input:unknown){const raw=this.object(input),allowed=["strategyId","evaluationQueryId","source","results","requestedResultCount","latencyMs","stageTraceId","providerMetadata"];if(Object.keys(raw).some(key=>!allowed.includes(key)))throw invalid("Execution input contains unsupported fields");const detail=await this.datasets.getDatasetDetail(userId,datasetId);if(detail.dataset.status!=="frozen")throw new EvaluationError("DATASET_NOT_FROZEN","Strategy execution requires a frozen dataset",409);const strategy=await this.getStrategy(userId,this.text(raw.strategyId,"strategyId",64));if(strategy.status!=="active")throw new EvaluationError("INVALID_STATE","Archived strategies cannot receive executions",409);const evaluationQueryId=this.text(raw.evaluationQueryId,"evaluationQueryId",64),query=detail.queries.find(item=>item.id===evaluationQueryId);if(!query)throw new EvaluationError("NOT_FOUND","Evaluation query is not in the dataset",404);if(!Array.isArray(raw.results)||!raw.results.length||raw.results.length>POLICY.maximumResultsPerExecution)throw invalid(`results must contain 1-${POLICY.maximumResultsPerExecution} documents`);for(const item of raw.results as Array<Record<string,unknown>>)if(item&&("documentKey" in item||"canonicalUrl" in item||"rank" in item))throw invalid("Server derives canonical identity and ranks");const prepared=canonicalizeStageDocuments((raw.results as Array<Record<string,unknown>>).map(item=>({url:item.url as string,score:item.score as number|undefined,scoreType:item.scoreType as string|undefined,title:item.title as string|undefined,domain:item.domain as string|undefined}))),latencyMs=raw.latencyMs===undefined||raw.latencyMs===null?null:Number(raw.latencyMs);if(latencyMs!==null&&(!Number.isFinite(latencyMs)||latencyMs<0))throw invalid("latencyMs must be non-negative");const source=raw.source as StrategyExecutionSource;if(source!=="native"&&source!=="imported")throw invalid("Execution source is invalid");const stageTraceId=this.optional(raw.stageTraceId);if(stageTraceId){const trace=await this.traces.get(userId,stageTraceId);if(trace.datasetVersionId!==datasetId||trace.evaluationQueryId!==query.id||trace.sourceQueryId!==query.sourceQueryId)throw new EvaluationError("INVALID_STATE","Stage trace is incompatible with strategy execution",409);const final=trace.stages.find(stage=>stage.type==="final");if(final&&(final.documents.length!==prepared.documents.length||final.documents.some((document,index)=>document.documentKey!==prepared.documents[index].documentKey||document.rank!==prepared.documents[index].rank)))throw new EvaluationError("INVALID_STATE","Stage trace final ranking does not match imported strategy results",409)}const executionId=ID.unique(),createdAt=new Date(),metadata=this.configuration(raw.providerMetadata);try{for(const document of prepared.documents)await this.repository.createExecutionDocument(ID.unique(),{executionId,documentKey:document.documentKey,canonicalUrl:document.canonicalUrl,rawUrl:document.rawUrl,rank:document.rank,score:document.score===null?null:String(document.score),scoreType:document.scoreType,title:document.title,domain:document.domain});const header=await this.repository.createExecution(executionId,{strategyId:strategy.id,datasetVersionId:datasetId,evaluationQueryId:query.id,sourceQueryId:query.sourceQueryId,queryText:query.queryText,source,configHash:strategy.configHash,requestedResultCount:raw.requestedResultCount??null,resultCount:prepared.documents.length,latencyMs:latencyMs===null?null:String(latencyMs),latencyType:strategy.latencyType,stageTraceId,providerMetadataJson:JSON.stringify(metadata),duplicateCanonicalResultsIgnored:prepared.duplicates,createdAt:createdAt.toISOString(),createdByUserId:userId});await this.repository.updateStrategy(strategy.id,{executionCount:strategy.executionCount+1});return transformExecution(header,await this.repository.listExecutionDocuments(executionId))}catch(error){await Promise.all([this.repository.deleteExecution(executionId).catch(()=>undefined),this.repository.deleteExecutionDocuments(executionId).catch(()=>undefined)]);throw error}}
 async getExecution(userId:string,id:string){const header=await this.repository.getExecution(id);if(!header)throw new EvaluationError("NOT_FOUND","Strategy execution not found",404);const result=transformExecution(header,await this.repository.listExecutionDocuments(id));if(result.createdByUserId!==userId)throw new EvaluationError("UNAUTHORIZED","Strategy execution access denied",403);return result}
 async listExecutions(userId:string,datasetId:string,strategyId?:string,evaluationQueryId?:string){const docs=await this.repository.listExecutions(datasetId,strategyId,evaluationQueryId),results=[];for(const header of docs){const result=transformExecution(header,await this.repository.listExecutionDocuments(required(header,"$id")));if(result.createdByUserId!==userId)throw new EvaluationError("UNAUTHORIZED","Strategy execution listing contains foreign records",403);results.push(result)}return results}
 private object(value:unknown){if(!value||typeof value!=="object"||Array.isArray(value))throw invalid("Input must be an object");return value as Record<string,unknown>}
 private text(value:unknown,name:string,max:number){if(typeof value!=="string"||!value.trim()||value.length>max)throw invalid(`${name} is required`);return value.trim()}
 private optional(value:unknown){if(value===undefined||value===null||value==="")return null;if(typeof value!=="string")throw invalid("Optional text field is invalid");return value.trim()}
 private configuration(value:unknown){if(value===undefined)return{};if(!value||typeof value!=="object"||Array.isArray(value))throw invalid("Configuration must be an object");const serialized=JSON.stringify(value);if(Buffer.byteLength(serialized)>16_384)throw new EvaluationError("PROVENANCE_LIMIT","Configuration exceeds storage limit",413);return value as Record<string,unknown>}
}
export const evaluationStrategyService=new EvaluationStrategyService()
