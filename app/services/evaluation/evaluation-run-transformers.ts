import type { AggregateEvaluationResult, AggregateMetricValue, EvaluationAtCutoff, MetricValue, PerQueryEvaluationResult } from "./metrics/types"
import type { EvaluationRun, EvaluationRunSnapshotSelection, EvaluationRunSummary } from "@/types/evaluation-runs"

type Document = Record<string, unknown>
const required=(doc:Document,key:string)=>{const value=doc[key];if(typeof value!=="string"||!value.trim())throw new TypeError(`evaluation run ${key} is required`);return value}
const integer=(value:unknown,name:string,min=0)=>{if(!Number.isInteger(value)||Number(value)<min)throw new TypeError(`${name} must be an integer >= ${min}`);return Number(value)}
const json=(doc:Document,key:string)=>{const raw=required(doc,key);try{return JSON.parse(raw) as unknown}catch{throw new TypeError(`evaluation run ${key} is malformed JSON`)}}
const date=(value:unknown,name:string)=>{if(typeof value!=="string")throw new TypeError(`${name} is required`);const parsed=new Date(value);if(Number.isNaN(parsed.getTime()))throw new TypeError(`${name} is invalid`);return parsed}
const finite=(value:unknown,name:string)=>{if(typeof value!=="number"||!Number.isFinite(value))throw new TypeError(`${name} must be finite`);return value}
const score=(value:unknown,name:string)=>{const parsed=finite(value,name);if(parsed<0||parsed>1)throw new TypeError(`${name} must be between 0 and 1`);return parsed}
const exactKeys=(value:Record<string,unknown>,allowed:string[],name:string)=>{if(Object.keys(value).some(key=>!allowed.includes(key)))throw new TypeError(`${name} contains unsupported fields`)}
const object=(value:unknown,name:string):Record<string,unknown>=>{if(!value||typeof value!=="object"||Array.isArray(value))throw new TypeError(`${name} must be an object`);return value as Record<string,unknown>}

function metricValue(value:unknown,name:string):MetricValue{const v=object(value,name);exactKeys(v,["value","eligible","reason"],name);if(v.value!==null)score(v.value,`${name}.value`);if(typeof v.eligible!=="boolean")throw new TypeError(`${name}.eligible is invalid`);if(v.reason!==undefined&&typeof v.reason!=="string")throw new TypeError(`${name}.reason is invalid`);if((v.value===null)===v.eligible)throw new TypeError(`${name} availability is inconsistent`);return v as unknown as MetricValue}
function aggregateMetric(value:unknown,name:string):AggregateMetricValue{const v=object(value,name);exactKeys(v,["value","eligibleQueryCount"],name);if(v.value!==null)score(v.value,`${name}.value`);const count=integer(v.eligibleQueryCount,`${name}.eligibleQueryCount`);if((v.value===null)!==(count===0))throw new TypeError(`${name} availability is inconsistent`);return v as unknown as AggregateMetricValue}
function aggregate(value:unknown):AggregateEvaluationResult{const v=object(value,"aggregate");exactKeys(v,["metricVersion","queryCount","eligibleQueryCount","skippedQueryCount","mrr","byCutoff","warnings"],"aggregate");required(v,"metricVersion");const queryCount=integer(v.queryCount,"aggregate.queryCount"),eligibleQueryCount=integer(v.eligibleQueryCount,"aggregate.eligibleQueryCount"),skippedQueryCount=integer(v.skippedQueryCount,"aggregate.skippedQueryCount");if(queryCount!==eligibleQueryCount+skippedQueryCount)throw new TypeError("aggregate query counts are inconsistent");const mrr=aggregateMetric(v.mrr,"aggregate.mrr");if(!Array.isArray(v.byCutoff)||!Array.isArray(v.warnings)||v.warnings.some(w=>typeof w!=="string"))throw new TypeError("aggregate arrays are malformed");const byCutoff=v.byCutoff.map((raw,index)=>{const item=object(raw,`aggregate.byCutoff[${index}]`);exactKeys(item,["cutoff","meanNdcg","meanBenchmarkRecall","meanHit","meanJudgedPrecision","meanJudgmentCoverage"],`aggregate.byCutoff[${index}]`);integer(item.cutoff,"aggregate cutoff",1);return{cutoff:Number(item.cutoff),meanNdcg:aggregateMetric(item.meanNdcg,"meanNdcg"),meanBenchmarkRecall:aggregateMetric(item.meanBenchmarkRecall,"meanBenchmarkRecall"),meanHit:aggregateMetric(item.meanHit,"meanHit"),meanJudgedPrecision:aggregateMetric(item.meanJudgedPrecision,"meanJudgedPrecision"),meanJudgmentCoverage:aggregateMetric(item.meanJudgmentCoverage,"meanJudgmentCoverage")}});return{metricVersion:String(v.metricVersion) as "1",queryCount,eligibleQueryCount,skippedQueryCount,mrr,byCutoff,warnings:[...v.warnings] as string[]}}
function cutoffMetric(value:unknown,index:number):EvaluationAtCutoff{const v=object(value,`perQuery.metrics[${index}]`);exactKeys(v,["cutoff","ndcg","benchmarkRecall","hit","judgedPrecision","judgmentCoverage","counts"],"cutoff metric");integer(v.cutoff,"cutoff",1);score(v.hit,"hit");const c=object(v.counts,"counts");const names=["evaluatedTopK","judged","judgedRelevant","judgedIrrelevant","unjudged","knownRelevantBenchmarkDocuments","duplicateCanonicalResultsIgnored"] as const;exactKeys(c,[...names],"counts");const counts=Object.fromEntries(names.map(name=>[name,integer(c[name],name)])) as unknown as EvaluationAtCutoff["counts"];if(counts.judged!==counts.judgedRelevant+counts.judgedIrrelevant||counts.evaluatedTopK!==counts.judged+counts.unjudged)throw new TypeError("per-query metric counts are inconsistent");return{cutoff:Number(v.cutoff),ndcg:metricValue(v.ndcg,"ndcg"),benchmarkRecall:metricValue(v.benchmarkRecall,"benchmarkRecall"),hit:Number(v.hit),judgedPrecision:metricValue(v.judgedPrecision,"judgedPrecision"),judgmentCoverage:metricValue(v.judgmentCoverage,"judgmentCoverage"),counts}}
export function parsePerQueryResult(value:unknown):PerQueryEvaluationResult{const v=object(value,"perQuery result");exactKeys(v,["datasetVersionId","evaluationQueryId","sourceQueryId","snapshotId","metricVersion","eligible","reciprocalRank","metrics","warnings"],"perQuery result");for(const key of ["datasetVersionId","evaluationQueryId","sourceQueryId","snapshotId","metricVersion"])required(v,key);if(typeof v.eligible!=="boolean")throw new TypeError("perQuery eligible is invalid");finite(v.reciprocalRank,"reciprocalRank");if(!Array.isArray(v.metrics)||!Array.isArray(v.warnings)||v.warnings.some(w=>typeof w!=="string"))throw new TypeError("perQuery arrays are malformed");return{datasetVersionId:String(v.datasetVersionId),evaluationQueryId:String(v.evaluationQueryId),sourceQueryId:String(v.sourceQueryId),snapshotId:String(v.snapshotId),metricVersion:String(v.metricVersion) as "1",eligible:v.eligible,reciprocalRank:Number(v.reciprocalRank),metrics:v.metrics.map(cutoffMetric),warnings:[...v.warnings] as string[]}}
function cutoffs(value:unknown):number[]{if(!Array.isArray(value)||!value.length)return (()=>{throw new TypeError("cutoffs must be a non-empty array")})();const result=value.map((item,index)=>integer(item,`cutoffs[${index}]`,1));if(new Set(result).size!==result.length||result.some((item,index)=>index>0&&item<result[index-1]))throw new TypeError("cutoffs must be unique and sorted");return result}
function selections(value:unknown):EvaluationRunSnapshotSelection[]{if(!Array.isArray(value)||!value.length)throw new TypeError("snapshot selections must be a non-empty array");const result=value.map((raw,index)=>{const v=object(raw,`selection[${index}]`);exactKeys(v,["evaluationQueryId","snapshotId"],"selection");return{evaluationQueryId:required(v,"evaluationQueryId"),snapshotId:required(v,"snapshotId")}});if(new Set(result.map(item=>item.evaluationQueryId)).size!==result.length)throw new TypeError("snapshot selections contain duplicate queries");return result}

function header(doc:Document){const id=required(doc,"$id"),datasetVersionId=required(doc,"datasetVersionId"),datasetFamilyKey=required(doc,"datasetFamilyKey"),datasetVersion=integer(doc.datasetVersion,"datasetVersion",1),metricVersion=required(doc,"metricVersion"),status=required(doc,"status");if(status!=="completed")throw new TypeError("evaluation run status is invalid");const parsedCutoffs=cutoffs(json(doc,"cutoffsJson")),snapshotSelections=selections(json(doc,"snapshotSelectionsJson")),parsedAggregate=aggregate(json(doc,"aggregateResultJson")),warnings=json(doc,"warningsJson");if(!Array.isArray(warnings)||warnings.some(item=>typeof item!=="string"))throw new TypeError("warningsJson is malformed");const eligibleQueryCount=integer(doc.eligibleQueryCount,"eligibleQueryCount"),skippedQueryCount=integer(doc.skippedQueryCount,"skippedQueryCount"),selectedQueryCount=integer(doc.selectedQueryCount,"selectedQueryCount");if(selectedQueryCount!==snapshotSelections.length||eligibleQueryCount!==parsedAggregate.eligibleQueryCount||skippedQueryCount!==parsedAggregate.skippedQueryCount)throw new TypeError("evaluation run counts are inconsistent");if(metricVersion!==parsedAggregate.metricVersion||JSON.stringify(parsedCutoffs)!==JSON.stringify(parsedAggregate.byCutoff.map(item=>item.cutoff))||JSON.stringify(warnings)!==JSON.stringify(parsedAggregate.warnings))throw new TypeError("evaluation run policy, cutoffs, or warnings are inconsistent");return{id,datasetVersionId,datasetFamilyKey,datasetVersion,metricVersion,status:"completed" as const,cutoffs:parsedCutoffs,snapshotSelections,aggregate:parsedAggregate,warnings:[...warnings] as string[],eligibleQueryCount,skippedQueryCount,selectedQueryCount,createdAt:date(doc.createdAt,"createdAt"),createdByUserId:required(doc,"createdByUserId")}}
export function transformEvaluationRunSummary(doc:Document):EvaluationRunSummary{const h=header(doc);return{id:h.id,datasetVersionId:h.datasetVersionId,datasetFamilyKey:h.datasetFamilyKey,datasetVersion:h.datasetVersion,metricVersion:h.metricVersion,status:h.status,cutoffs:h.cutoffs,selectedQueryCount:h.selectedQueryCount,eligibleQueryCount:h.eligibleQueryCount,skippedQueryCount:h.skippedQueryCount,aggregate:{mrr:h.aggregate.mrr,byCutoff:h.aggregate.byCutoff},createdAt:h.createdAt,createdByUserId:h.createdByUserId}}
export function transformEvaluationRun(doc:Document,queryDocs:Document[]):EvaluationRun{const h=header(doc),selectionMap=new Map(h.snapshotSelections.map(item=>[item.evaluationQueryId,item.snapshotId]));const perQuery=queryDocs.map(item=>{if(required(item,"runId")!==h.id||required(item,"datasetVersionId")!==h.datasetVersionId)throw new TypeError("evaluation run query is orphaned");const result=parsePerQueryResult(json(item,"resultJson"));if(result.datasetVersionId!==h.datasetVersionId||result.metricVersion!==h.metricVersion||selectionMap.get(result.evaluationQueryId)!==result.snapshotId||required(item,"evaluationQueryId")!==result.evaluationQueryId||required(item,"snapshotId")!==result.snapshotId||JSON.stringify(result.metrics.map(metric=>metric.cutoff))!==JSON.stringify(h.cutoffs))throw new TypeError("evaluation run query provenance is inconsistent");return result});if(perQuery.length!==h.selectedQueryCount||perQuery.length!==h.aggregate.queryCount||new Set(perQuery.map(item=>item.evaluationQueryId)).size!==perQuery.length)throw new TypeError("evaluation run query count is inconsistent");return{...h,perQuery}}
