jest.mock("@/app/server/appwrite/appwrite-server", () => ({
  databases: {}, DATABASE_ID: "test",
  COLLECTIONS: { EVALUATION_DATASETS: "datasets", EVALUATION_QUERIES: "queries", QUERIES: "operational" },
  ID: { unique: (() => { let n = 0; return () => `generated${++n}` })() },
  Query: { equal: jest.fn(), orderDesc: jest.fn(), orderAsc: jest.fn(), limit: jest.fn(), offset: jest.fn() },
}))

import { EvaluationDatasetService, type DatasetListOptions, type EvaluationRepository, type OperationalQueryReader } from "../evaluation-dataset-service"
import { EvaluationError } from "../evaluation-errors"
import { CANONICALIZATION_VERSION, createEvaluationQueryKey } from "@/utils/canonicalize-document-url"
import { computeConfigHash } from "@/utils/coverage-and-versioning"
import type { EvaluationDatasetVersion, EvaluationQuery, RelevanceJudgment } from "@/types/evaluation"
import type { QueryConfig } from "@/types/type"

const copy = <T>(value:T):T => structuredClone(value)
class MemoryRepository implements EvaluationRepository {
  datasets: EvaluationDatasetVersion[]=[]; queries: EvaluationQuery[]=[]; judgments:RelevanceJudgment[]=[]; sequence=0; raceOnNextVersion=false
  async createDataset(data:Omit<EvaluationDatasetVersion,"id">) { if(this.raceOnNextVersion && data.version > 1) { this.raceOnNextVersion=false; this.datasets.push({...copy(data),id:`racer-${++this.sequence}`}); throw new EvaluationError("CONFLICT","version race",409) } if(this.datasets.some(d=>d.familyKey===data.familyKey&&d.version===data.version)) throw new EvaluationError("CONFLICT","duplicate",409); const item={...copy(data),id:`dataset-${++this.sequence}`};this.datasets.push(item);return copy(item) }
  async updateDataset(id:string,data:Partial<Pick<EvaluationDatasetVersion,"queryCount"|"judgmentCount"|"conflictCount"|"status"|"frozenAt"|"frozenByUserId"|"updatedAt">>) { const item=this.datasets.find(d=>d.id===id)!;Object.assign(item,copy(data));return copy(item) }
  async getDataset(id:string) { return copy(this.datasets.find(d=>d.id===id)??null) }
  async listDatasets(owner:string,options:DatasetListOptions) { return copy(this.datasets.filter(d=>d.ownerUserId===owner&&(!options.status||d.status===options.status)&&(!options.familyKey||d.familyKey===options.familyKey)).sort((a,b)=>b.createdAt.getTime()-a.createdAt.getTime()).slice(options.offset,options.offset+options.limit)) }
  async listFamily(owner:string,family:string) { return copy(this.datasets.filter(d=>d.ownerUserId===owner&&d.familyKey===family)) }
  async listQueries(datasetId:string) { return copy(this.queries.filter(q=>q.datasetVersionId===datasetId)) }
  async createQuery(data:Omit<EvaluationQuery,"id">) { const found=this.queries.find(q=>q.queryKey===data.queryKey);if(found)return copy(found);const item={...copy(data),id:`evaluation-query-${++this.sequence}`};this.queries.push(item);return copy(item) }
  async getQuery(id:string){return copy(this.queries.find(q=>q.id===id)??null)}
  async getJudgment(){return null}
  async getJudgmentByKey(){return null}
  async listJudgments(datasetId:string,queryId?:string){return copy(this.judgments.filter(j=>j.datasetVersionId===datasetId&&(!queryId||j.evaluationQueryId===queryId)))}
  async createJudgment():Promise<never>{throw new Error("unused")}
  async updateJudgment():Promise<never>{throw new Error("unused")}
}
class MemoryQueryReader implements OperationalQueryReader {
  constructor(public queries=new Map<string,QueryConfig>()){}
  async getQuery(id:string){return copy(this.queries.get(id)??null)}
}
const operational=(overrides:Partial<QueryConfig>={}):QueryConfig=>({id:"q1",name:"Vector research",query:"latest vector database filtering research",category:"research_paper",filters:{includeDomains:["example.com"],excludeDomains:["spam.test"],startDate:"2025-01-01",endDate:"2026-01-01",numResults:25},schedule:{enabled:true,frequency:"daily"},tags:["ignored"],createdAt:new Date(),userId:"owner",...overrides})
const setup=()=>{const repo=new MemoryRepository();const reader=new MemoryQueryReader(new Map([["q1",operational()]]));return{repo,reader,service:new EvaluationDatasetService(repo,reader)}}

describe("EvaluationDatasetService",()=>{
  test("creates server-authoritative version 1 draft",async()=>{const{service}=setup();const value=await service.createDataset("owner",{name:"Core Search",familyKey:"Core Search"});expect(value).toMatchObject({familyKey:"core-search",version:1,status:"draft",ownerUserId:"owner",createdByUserId:"owner",queryCount:0,judgmentCount:0,conflictCount:0,canonicalizationVersion:CANONICALIZATION_VERSION})})
  test.each(["", " "])("rejects invalid name %p",async name=>{await expect(setup().service.createDataset("owner",{name})).rejects.toMatchObject({code:"INVALID_INPUT"})})
  test("rejects invalid supplied family key",async()=>{await expect(setup().service.createDataset("owner",{name:"Core",familyKey:"---"})).rejects.toMatchObject({code:"INVALID_INPUT"})})
  test("snapshots authoritative query fields and is idempotent",async()=>{const{service}=setup();const dataset=await service.createDataset("owner",{name:"Core",familyKey:"core"});const first=await service.addOperationalQueries("owner",dataset.id,["q1"]);expect(first.added).toBe(1);expect(first.queries[0]).toMatchObject({datasetVersionId:dataset.id,sourceQueryId:"q1",queryKey:createEvaluationQueryKey(dataset.id,"q1"),name:"Vector research",queryText:"latest vector database filtering research",category:"research_paper",filters:{includeDomains:["example.com"],excludeDomains:["spam.test"],numResults:25},configHash:computeConfigHash(operational())});const second=await service.addOperationalQueries("owner",dataset.id,["q1","q1"]);expect(second.added).toBe(0);expect(second.queries).toHaveLength(1);expect(second.dataset.queryCount).toBe(1)})
  test("rejects another owner's operational query",async()=>{const{service,reader}=setup();reader.queries.set("foreign",operational({id:"foreign",userId:"other"}));const dataset=await service.createDataset("owner",{name:"Core"});await expect(service.addOperationalQueries("owner",dataset.id,["foreign"])).rejects.toMatchObject({code:"UNAUTHORIZED"})})
  test("cannot add to a non-draft dataset",async()=>{const{service,repo}=setup();const dataset=await service.createDataset("owner",{name:"Core"});repo.datasets[0].status="frozen";repo.datasets[0].frozenAt=new Date();repo.datasets[0].frozenByUserId="owner";await expect(service.addOperationalQueries("owner",dataset.id,["q1"])).rejects.toMatchObject({code:"DATASET_NOT_DRAFT"})})
  test("benchmark copy survives live edit and deletion",async()=>{const{service,reader}=setup();const dataset=await service.createDataset("owner",{name:"Core"});const saved=(await service.addOperationalQueries("owner",dataset.id,["q1"])).queries[0];reader.queries.set("q1",operational({query:"edited",filters:{numResults:99}}));expect((await service.getDatasetDetail("owner",dataset.id)).queries[0]).toEqual(saved);reader.queries.delete("q1");expect((await service.getDatasetDetail("owner",dataset.id)).queries[0]).toEqual(saved)})
  test("clones only frozen versions from benchmark copies",async()=>{const{service,repo,reader}=setup();const parent=await service.createDataset("owner",{name:"Core",familyKey:"core"});await service.addOperationalQueries("owner",parent.id,["q1"]);await expect(service.cloneFrozenDataset("owner",parent.id)).rejects.toMatchObject({code:"DATASET_NOT_FROZEN"});repo.datasets[0].status="frozen";repo.datasets[0].frozenAt=new Date();repo.datasets[0].frozenByUserId="owner";reader.queries.set("q1",operational({query:"live edit must not leak"}));const clone=await service.cloneFrozenDataset("owner",parent.id);expect(clone.dataset).toMatchObject({familyKey:"core",version:2,status:"draft",parentVersionId:parent.id,queryCount:1,judgmentCount:0,conflictCount:0});expect(clone.queries[0]).toMatchObject({datasetVersionId:clone.dataset.id,queryText:"latest vector database filtering research",queryKey:createEvaluationQueryKey(clone.dataset.id,"q1")})})
  test("retries a family-version allocation race",async()=>{const{service,repo}=setup();const parent=await service.createDataset("owner",{name:"Core",familyKey:"core"});repo.datasets[0].status="frozen";repo.datasets[0].frozenAt=new Date();repo.datasets[0].frozenByUserId="owner";repo.raceOnNextVersion=true;const clone=await service.cloneFrozenDataset("owner",parent.id);expect(clone.dataset.version).toBe(3)})
  test("enforces dataset ownership for reads, adds, and clone",async()=>{const{service,repo}=setup();const dataset=await service.createDataset("owner",{name:"Core"});await expect(service.getDatasetDetail("other",dataset.id)).rejects.toMatchObject({code:"UNAUTHORIZED"});await expect(service.addOperationalQueries("other",dataset.id,["q1"])).rejects.toMatchObject({code:"UNAUTHORIZED"});repo.datasets[0].status="frozen";repo.datasets[0].frozenAt=new Date();repo.datasets[0].frozenByUserId="owner";await expect(service.cloneFrozenDataset("other",dataset.id)).rejects.toMatchObject({code:"UNAUTHORIZED"})})
  test("reports query-foundation readiness without claiming full readiness",async()=>{const{service}=setup();const dataset=await service.createDataset("owner",{name:"Core"});expect((await service.getDatasetDetail("owner",dataset.id)).readiness).toMatchObject({queryFoundationReady:false,fullEvaluationFreezeReady:false});await service.addOperationalQueries("owner",dataset.id,["q1"]);expect((await service.getDatasetDetail("owner",dataset.id)).readiness).toMatchObject({queryFoundationReady:true,fullEvaluationFreezeReady:false})})
  test("freezes only an owner-scoped, fully ready draft",async()=>{const{service,repo}=setup();const dataset=await service.createDataset("owner",{name:"Core"});await service.addOperationalQueries("owner",dataset.id,["q1"]);const query=repo.queries[0];const now=new Date();repo.judgments.push({id:"j1",judgmentKey:"key",datasetVersionId:dataset.id,evaluationQueryId:query.id,sourceQueryId:"q1",documentKey:"doc",canonicalUrl:"https://example.com/",domain:"example.com",status:"accepted",relevanceGrade:2,source:"direct_label",assessments:[],sourceFeedbackIds:[],sourceSnapshotIds:["s1"],observedRawUrls:["https://example.com/"],observedContentHashes:[],createdAt:now,createdByUserId:"owner",updatedAt:now,updatedByUserId:"owner",acceptedAt:now,acceptedByUserId:"owner"});repo.datasets[0].judgmentCount=1;expect(service.freezeReadiness(repo.datasets[0],repo.queries,repo.judgments)).toMatchObject({queryFoundationReady:true,judgmentFoundationReady:true,fullEvaluationFreezeReady:true,reasons:[]});await expect(service.freezeDataset("other",dataset.id)).rejects.toMatchObject({code:"UNAUTHORIZED"});const frozen=await service.freezeDataset("owner",dataset.id);expect(frozen.dataset).toMatchObject({status:"frozen",frozenByUserId:"owner"});expect(frozen.dataset.frozenAt).toBeDefined();await expect(service.freezeDataset("owner",dataset.id)).rejects.toMatchObject({code:"DATASET_NOT_DRAFT"})})
  test("freeze rejects conflicts, count mismatches, orphan judgments, and missing accepted coverage",async()=>{const{service,repo}=setup();const dataset=await service.createDataset("owner",{name:"Core"});await expect(service.freezeDataset("owner",dataset.id)).rejects.toMatchObject({code:"INVALID_STATE"});await service.addOperationalQueries("owner",dataset.id,["q1"]);await expect(service.freezeDataset("owner",dataset.id)).rejects.toMatchObject({code:"INVALID_STATE"});const query=repo.queries[0],now=new Date();const base:RelevanceJudgment={id:"j",judgmentKey:"key",datasetVersionId:dataset.id,evaluationQueryId:query.id,sourceQueryId:"q1",documentKey:"doc",canonicalUrl:"https://example.com/",domain:"example.com",status:"accepted",relevanceGrade:1,source:"direct_label",assessments:[],sourceFeedbackIds:[],sourceSnapshotIds:[],observedRawUrls:[],observedContentHashes:[],createdAt:now,createdByUserId:"owner",updatedAt:now,updatedByUserId:"owner",acceptedAt:now,acceptedByUserId:"owner"};repo.judgments=[base];await expect(service.freezeDataset("owner",dataset.id)).rejects.toMatchObject({code:"INVALID_STATE"});repo.datasets[0].judgmentCount=1;repo.judgments=[{...base,evaluationQueryId:"orphan"}];await expect(service.freezeDataset("owner",dataset.id)).rejects.toMatchObject({code:"INVALID_STATE"});repo.judgments=[{...base,status:"conflicted",relevanceGrade:null,acceptedAt:undefined,acceptedByUserId:undefined}];repo.datasets[0].judgmentCount=0;repo.datasets[0].conflictCount=1;await expect(service.freezeDataset("owner",dataset.id)).rejects.toMatchObject({code:"INVALID_STATE"})})
})
