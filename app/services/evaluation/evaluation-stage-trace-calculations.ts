import {getDocumentIdentity} from "@/utils/canonicalize-document-url"
import type {DocumentStagePath,EvaluationExecutionTrace,EvaluationStageDocument,EvaluationStageTrace,StageTransition,StageTransitionType} from "@/types/evaluation-stage-trace"
import {EVALUATION_STAGE_TRACE_POLICY as POLICY} from "./stage-trace-policy"
export interface RawStageDocument{url:string;rank?:number|null;score?:number|null;scoreType?:string|null;title?:string|null;domain?:string|null;contentHash?:string|null;metadata?:Record<string,unknown>}
export function canonicalizeStageDocuments(input:RawStageDocument[]):{documents:EvaluationStageDocument[];duplicates:number}{if(input.length>POLICY.maxDocumentsPerStage)throw new TypeError(`Stage exceeds ${POLICY.maxDocumentsPerStage} documents`);const ranks=new Set<number>(),seen=new Set<string>(),documents:EvaluationStageDocument[]=[];let duplicates=0;const ordered=input.map((raw,index)=>({raw,index,rank:raw.rank===undefined?index+1:raw.rank})).sort((a,b)=>(a.rank??Number.MAX_SAFE_INTEGER)-(b.rank??Number.MAX_SAFE_INTEGER)||a.index-b.index);for(const entry of ordered){const raw=entry.raw,index=entry.index;if(!raw||typeof raw.url!=="string"||!raw.url.trim())throw new TypeError("Stage document URL is required");const rank=entry.rank;if(rank!==null&&(!Number.isInteger(rank)||rank<1))throw new TypeError("Stage document rank must be a positive integer or null");if(rank!==null&&ranks.has(rank))throw new TypeError("Ranked stage contains a duplicate rank");if(rank!==null)ranks.add(rank);if(raw.score!==undefined&&raw.score!==null&&!Number.isFinite(raw.score))throw new TypeError("Stage document score must be finite");const identity=getDocumentIdentity(raw.url);if(seen.has(identity.documentKey)){duplicates++;continue}seen.add(identity.documentKey);documents.push({documentKey:identity.documentKey,canonicalUrl:identity.canonicalUrl,rawUrl:raw.url,rank,score:raw.score??null,scoreType:raw.scoreType?.trim()||null,title:raw.title?.trim()||null,domain:raw.domain?.trim()||new URL(identity.canonicalUrl).hostname,contentHash:raw.contentHash?.trim()||null,metadata:raw.metadata??{},relevanceGrade:null,relevanceMeaning:"unjudged"})}return{documents,duplicates}}
export function transition(previous:{present:boolean;rank:number|null},next:{present:boolean;rank:number|null}):StageTransitionType{if(previous.present&&!next.present)return"lost";if(!previous.present&&next.present)return"entered";if(!previous.present&&!next.present)return"unknown";if(previous.rank===null||next.rank===null)return"retained";if(previous.rank===next.rank)return"unchanged";return previous.rank>next.rank?"promoted":"demoted"}
export function documentStagePath(trace:EvaluationExecutionTrace,documentKey:string):DocumentStagePath{const ordered=[...trace.stages].sort((a,b)=>a.order-b.order),found=ordered.flatMap(stage=>stage.documents.filter(document=>document.documentKey===documentKey));const representative=found.at(-1);if(!representative)throw new TypeError("Document is not present in any recorded stage");const stages=ordered.map(stage=>{const document=stage.documents.find(item=>item.documentKey===documentKey);return{stageId:stage.id,stageType:stage.type,stageRecorded:true as const,present:Boolean(document),rank:document?.rank??null,score:document?.score??null,scoreType:document?.scoreType??null}});const transitions:StageTransition[]=stages.slice(1).map((next,index)=>{const previous=stages[index],type=transition(previous,next);return{fromStageId:previous.stageId,toStageId:next.stageId,type,previousRank:previous.rank,nextRank:next.rank,rankDelta:previous.present&&next.present&&previous.rank!==null&&next.rank!==null?previous.rank-next.rank:null}});return{documentKey,canonicalUrl:representative.canonicalUrl,title:representative.title,relevanceGrade:representative.relevanceGrade,relevanceMeaning:representative.relevanceMeaning,stages,transitions}}
export function alignFinalStage(stage:EvaluationStageTrace,snapshotDocuments:EvaluationStageDocument[]){if(stage.type!=="final")throw new TypeError("Snapshot alignment requires a final stage");if(stage.documents.length!==snapshotDocuments.length)throw new TypeError("Final stage does not contain the snapshot canonical document set");for(let index=0;index<snapshotDocuments.length;index++){const actual=stage.documents[index],expected=snapshotDocuments[index];if(actual.documentKey!==expected.documentKey||actual.rank!==expected.rank)throw new TypeError("Final stage canonical order or rank does not match snapshot")}}
