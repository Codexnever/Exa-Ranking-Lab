import {NextResponse,type NextRequest} from "next/server";import type {SecurityContext} from "@/types/type";import {withEnhancedSecurity} from "@/lib/middleware/security/security-middleware";import {assertRouteId} from "@/app/services/evaluation/evaluation-input-validation";import {evaluationStrategyBenchmarkService} from "@/app/services/evaluation/evaluation-strategy-benchmark-service";import {EvaluationError,invalid} from "@/app/services/evaluation/evaluation-errors"
async function handler(request:NextRequest,c:SecurityContext,route:{params:Promise<{id:string}>}){try{const{id}=await route.params;assertRouteId("dataset ID",id);const input=await request.json().catch(()=>{throw invalid("Request body must be valid JSON")});return NextResponse.json(await evaluationStrategyBenchmarkService.benchmark(c.user.$id,id,input))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Strategy benchmark failed"},{status:e instanceof EvaluationError?e.status:e instanceof TypeError?400:500})}}export const POST=withEnhancedSecurity(handler,{allowedMethods:["POST"],logAttempts:true})
